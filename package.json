{
  "name": "word-ninja",
  "private": true,
  "version": "1.0.0",
  "description": "An arcade word game: letters fly, you slice through them to build words against the clock.",
  "scripts": {
    "test": "node tests/smoke.mjs",
    "generate": "node tools/generate-words.mjs",
    "icons": "node tools/make-icons.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
